apiVersion: apps/v1
kind: Deployment
metadata:
  name: embeddings
  namespace: kg-experiments
spec:
  template:
    spec:
      containers:
      - name: tei
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1"
            memory: "512Mi"
