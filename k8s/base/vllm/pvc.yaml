apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-cache
  namespace: kg-experiments
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
